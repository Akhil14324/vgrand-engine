# Voice model service — reuse the pinned Chatterbox bake-off environment and
# add only the HTTP layer. Install into the same venv:
#   infra/voice-bakeoff/.venv-chatterbox/Scripts/pip install -r infra/voice-service/requirements.txt
-r ../voice-bakeoff/requirements-chatterbox-lock.txt
fastapi==0.115.14
uvicorn==0.30.6
python-multipart==0.0.9
soundfile==0.13.1
